{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env"],
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "api#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": ["OLLAMA_MODEL", "OLLAMA_HOST", "OLLAMA_API_KEY"]
    },
    "api#dev": {
      "dependsOn": ["^dev"],
      "cache": false,
      "persistent": true,
      "env": ["OLLAMA_MODEL", "OLLAMA_HOST", "OLLAMA_API_KEY"]
    },
    "web#dev": {
      "dependsOn": ["^dev"],
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "type-check": {
      "dependsOn": ["^build"]
    }
  }
}
